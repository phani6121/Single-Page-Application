export default class {//at here we are taking class and also constructor
    constructor() {

    }
    setTitle(title) {//This is for when we are open new page that title is changed 
        document.title = title;
    }
    async getHtml() {//This is for given data to sent the HTML view of the page 
        return "";//Those return data will displayed on the page  
    }
}